// import { createClient } from "@supabase/supabase-js";

// const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL;       // Same project URL
// const supabaseServiceKey = process.env.SUPABASE_SERVICE_ROLE_KEY; // Service role key (⚠️ keep secret)

// const supabaseAdmin = createClient(supabaseUrl, supabaseServiceKey);

// export default supabaseAdmin;
